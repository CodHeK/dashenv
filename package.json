{
  "name": "dashenv",
  "version": "1.0.0",
  "description": "Set your env variables, client/server side.",
  "main": "index.js",
  "keywords" : ["env", "dashenv", "evironment", ".env", "dotenv", "Dotenv", "npm", "package", "codhek"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security"
  }
}
